#
# ===============================
# Do not edit this file directly!
# ===============================
#
# Your changes will be overwritten when the Upptime template updates (by default, weekly)
# Instead, change .upptimerc.yml configuration and the workflows will be generated accordingly.
#
# 🔼 Upptime @v1.44.0
# GitHub-powered open-source uptime monitor and status page by Anand Chowdhary

# * Source: https://github.com/upptime/upptime
# * Docs and more: https://upptime.js.org
# * More by Anand Chowdhary: https://anandchowdhary.com


name: Response Time CI
on:
  schedule:
    - cron: "0 23 * * *"
  repository_dispatch:
    types: [response_time]
  workflow_dispatch:
concurrency:
  group: ${{ github.repository }}-${{ github.head_ref || github.ref_name }}-upptime
  cancel-in-progress: false
jobs:
  release:
    name: Check status
    runs-on: ubuntu-latest
    env:
      GH_APP_PRIVATE_KEY: ${{ secrets.GH_APP_PRIVATE_KEY }}
    steps:
      - name: Create GitHub App token
        id: app_token
        if: ${{ vars.GH_APP_ID != '' && env.GH_APP_PRIVATE_KEY != '' }}
        uses: actions/create-github-app-token@v3
        with:
          client-id: ${{ vars.GH_APP_ID }}
          private-key: ${{ env.GH_APP_PRIVATE_KEY }}
      - name: Clear GitHub App private key
        if: ${{ always() }}
        shell: bash
        run: echo "GH_APP_PRIVATE_KEY=" >> "$GITHUB_ENV"
      - name: Checkout
        uses: actions/checkout@v6
        with:
          ref: ${{ github.head_ref || github.ref_name }}
          token: ${{ steps.app_token.outputs.token || secrets.GH_PAT || github.token }}
      - name: Update response time
        uses: upptime/uptime-monitor@v1.44.0
        with:
          command: "response-time"
        env:
          GH_PAT: ${{ steps.app_token.outputs.token || secrets.GH_PAT || github.token }}
          # Configure the secret allowlist in .upptimerc.yml; do not edit this workflow directly.
          SECRETS_CONTEXT: '{"CORE_API_PATH":${{ toJson(secrets.CORE_API_PATH) }},"GRAPH_API_PATH":${{ toJson(secrets.GRAPH_API_PATH) }},"ACTION_ITEMS_API_PATH":${{ toJson(secrets.ACTION_ITEMS_API_PATH) }},"ATS_API_PATH":${{ toJson(secrets.ATS_API_PATH) }},"AUDIT_TRAIL_API_PATH":${{ toJson(secrets.AUDIT_TRAIL_API_PATH) }},"DISCUSSIONS_API_PATH":${{ toJson(secrets.DISCUSSIONS_API_PATH) }},"IDENTITY_OS_API_PATH":${{ toJson(secrets.IDENTITY_OS_API_PATH) }},"ONE_ON_ONES_API_PATH":${{ toJson(secrets.ONE_ON_ONES_API_PATH) }},"SETTINGS_API_PATH":${{ toJson(secrets.SETTINGS_API_PATH) }},"TALENT_MATCHING_API_PATH":${{ toJson(secrets.TALENT_MATCHING_API_PATH) }},"TIME_TRACKER_API_PATH":${{ toJson(secrets.TIME_TRACKER_API_PATH) }},"WORKFLOWS_OS_API_PATH":${{ toJson(secrets.WORKFLOWS_OS_API_PATH) }},"REPORTS_API_PATH":${{ toJson(secrets.REPORTS_API_PATH) }},"BILLING_API_PATH":${{ toJson(secrets.BILLING_API_PATH) }},"CLIENT_API_PATH":${{ toJson(secrets.CLIENT_API_PATH) }},"RDLC_API_PATH":${{ toJson(secrets.RDLC_API_PATH) }},"FUNCTION_CORE_API_PATH":${{ toJson(secrets.FUNCTION_CORE_API_PATH) }},"WORKFLOW_FUNCTIONS_API_PATH":${{ toJson(secrets.WORKFLOW_FUNCTIONS_API_PATH) }},"NOTIFICATIONS_FN_API_PATH":${{ toJson(secrets.NOTIFICATIONS_FN_API_PATH) }},"TME_FN_API_PATH":${{ toJson(secrets.TME_FN_API_PATH) }},"WORKFLOWS_STUDIO_PATH":${{ toJson(secrets.WORKFLOWS_STUDIO_PATH) }},"NOTIFICATION_TEAMS":${{ toJson(secrets.NOTIFICATION_TEAMS) }},"NOTIFICATION_TEAMS_WEBHOOK_URL":${{ toJson(secrets.NOTIFICATION_TEAMS_WEBHOOK_URL) }}}'
